use <led.scad>
// not sure if we'll use this

_nrows=5;
_ncols=10;
// space between LEDs in millimeters
_space=3;

// Takes row and column of led in array and gives the shaft height
function _led_shaft_height (r,c)=0;
